Shows your worker-session roster: id, state (`starting`/`running`/`idle`/`dead`), model, turn count, queued messages, and a one-line gist of each session's latest activity.

Use it to reorient: which sessions exist, who is busy, and who is idle and ready for the next instruction.
